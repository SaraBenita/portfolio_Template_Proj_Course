
export default class project {
    constructor(projectId, projectName, projectDescription, projectLink) {
        this.projectId = projectId; // מזהה ייחודי של הפרויקט
        this.projectName = projectName; // שם הפרויקט
        this.projectDescription = projectDescription; // תיאור הפרויקט
        this.projectLink = projectLink; // קישור לפרויקט
    }
}

